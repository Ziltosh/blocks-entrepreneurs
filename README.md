Il faut node et npm pour faire fonctionner le projet.

npm install pour installer les dépendances

npm run dev pour build en mode watch / développement

Chaque block est dans le dossier development/packages

